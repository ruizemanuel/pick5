{
  "name": "pick5",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "overrides": {
      "axios": "^1.15.1",
      "postcss": "^8.5.10"
    }
  }
}
